# 🚀 ULTRA BOT v3.0 - Ultimate Performance Edition

**Премиум версия бота для Murder Mystery 2 с максимальными улучшениями**

---

## ✨ Основные улучшения

### 🎯 Алгоритмы
- **Умная система навигации** с предсказанием траектории
- **Мультицелевая приоритизация** - выбирает лучшие цели автоматически
- **Адаптивная система порогов** с машинным обучением
- **Система памяти целей** - запоминает позиции для повторного сбора
- **Улучшенный PID контроллер** с адаптивными коэффициентами

### ⚡ Производительность
- **Оптимизация GPU/CPU** - автоматическое определение устройства
- **Три режима производительности**: Speed, Balanced, Accuracy
- **Мультискейл детекция** для лучшей точности
- **Умное кэширование** для уменьшения нагрузки
- **Профилирование производительности** в реальном времени

### 🎨 Визуализация
- **Красивая визуализация** с телеметрией
- **Отображение целей** и памяти
- **Статистика в реальном времени**
- **Информационная панель** с ключевыми метриками

### 🛡️ Надежность
- **Система антизастревания** с умными маневрами
- **Обработка ошибок** и восстановление
- **Стабильная работа** без зависаний
- **Гибкая настройка** параметров

---

## 📦 Установка

### Требования
- Python 3.8+
- Windows 10/11
- NVIDIA GPU (рекомендуется) или CPU
- Roblox установлен и запущен

### Зависимости
```bash
pip install ultralytics opencv-python pywin32 numpy pillow keyboard
```

Или используйте существующий `requirements.txt`:
```bash
pip install -r requirements.txt
pip install keyboard  # Добавляем поддержку горячих клавиш
```

---

## 🚀 Запуск

### Базовый запуск
```bash
python run_ultra_bot.py --weights weights/candies_v10.pt
```

### Запуск с параметрами
```bash
python run_ultra_bot.py \
    --weights weights/candies_v10.pt \
    --conf 0.22 \
    --mode balanced \
    --player-weights weights/peoples_yolov10m.pt
```

### Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--weights` | Путь к весам модели для монет | `weights/candies_v10.pt` |
| `--player-weights` | Путь к весам модели игроков (опционально) | `None` |
| `--conf` | Базовый порог уверенности | `0.22` |
| `--mode` | Режим производительности (`speed`, `balanced`, `accuracy`) | `balanced` |
| `--no-window` | Не показывать окно визуализации | `False` |

---

## ⚙️ Режимы производительности

### 🏃 Speed (Скорость)
- Максимальный FPS
- Быстрая детекция
- Минимальная задержка
```bash
python run_ultra_bot.py --mode speed
```

### ⚖️ Balanced (Сбалансированный)
- Оптимальный баланс скорости и точности
- Рекомендуется для большинства случаев
```bash
python run_ultra_bot.py --mode balanced
```

### 🎯 Accuracy (Точность)
- Максимальная точность детекции
- Мультискейл обработка
- Лучше для сложных сцен
```bash
python run_ultra_bot.py --mode accuracy
```

---

## 🎮 Управление

### Горячие клавиши
- **Q** или **ESC** - Остановка бота
- **Ctrl+C** - Мягкая остановка

### В окне визуализации
- **Q** - Выход
- **S** - Сохранить скриншот (если доступно)

---

## 📊 Статистика

Бот выводит статистику каждые 30 секунд:

```
📊 СТАТИСТИКА УЛЬТРА БОТА
============================================================
⏱️  Время работы: 120.5 сек
🎯 Детекций: 45
✨ Собрано: 38
📈 Эффективность: 84.4%
🔍 Циклов поиска: 12
💾 Память целей: 5
🔧 Застреваний: 2
📊 Средний FPS: 15.3
============================================================
```

---

## 🔧 Настройка параметров

### В коде можно настроить:

```python
# Параметры движения
self.min_distance = 48  # Минимальное расстояние для сбора
self.turn_threshold = 20  # Порог для поворота
self.approach_threshold = 180  # Порог приближения

# PID контроллер
self.pid['kp'] = 0.045  # Пропорциональный коэффициент
self.pid['ki'] = 0.0012  # Интегральный коэффициент
self.pid['kd'] = 0.028  # Дифференциальный коэффициент

# Система памяти
self.memory_retention = 3.0  # Время хранения памяти (секунды)
```

---

## 🆚 Сравнение с другими версиями

| Характеристика | Ultra Bot | Enhanced Bot | Base Bot |
|----------------|-----------|--------------|----------|
| Скорость | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Точность | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| Навигация | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| Память целей | ✅ | ✅ | ❌ |
| Предсказание траектории | ✅ | ❌ | ❌ |
| Профилирование | ✅ | ⚠️ Частично | ❌ |
| Визуализация | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🐛 Решение проблем

### Низкий FPS
1. Используйте режим `speed`: `--mode speed`
2. Уменьшите `predict_size` в коде
3. Проверьте использование GPU: `torch.cuda.is_available()`

### Плохая детекция
1. Уменьшите `--conf` до 0.15-0.20
2. Используйте режим `accuracy`: `--mode accuracy`
3. Проверьте качество весов модели

### Застревания
1. Бот автоматически определяет застревания
2. Выполняет маневры выхода
3. Можно настроить пороги в коде

### Ошибки GPU
1. Убедитесь, что установлены CUDA драйверы
2. Проверьте совместимость PyTorch и CUDA
3. Используйте CPU режим: отключите GPU в коде

---

## 📈 Оптимизация

### Для слабых ПК
```python
# В run_ultra_bot.py измените:
self.predict_size = 320
self.frame_skip = 2
self.use_half_precision = True
```

### Для мощных ПК
```python
# В run_ultra_bot.py измените:
self.predict_size = 640
self.frame_skip = 0
self.multi_scale = True
```

---

## 🎯 Рекомендации

1. **Начните с режима `balanced`** - оптимальный баланс
2. **Используйте GPU** для лучшей производительности
3. **Мониторьте статистику** для понимания работы бота
4. **Настройте параметры** под вашу систему
5. **Используйте визуализацию** для отладки

---

## 📝 Changelog

### v3.0 (Ultra Bot)
- ✨ Полностью переработанный алгоритм навигации
- ✨ Система памяти целей
- ✨ Предсказание траектории
- ✨ Улучшенная визуализация
- ✨ Профилирование производительности
- ✨ Три режима производительности
- ✨ Умное кэширование

---

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

## 💡 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь в корректности весов модели
3. Проверьте системные требования
4. Попробуйте разные параметры запуска

---

**Приятной игры! 🎮✨**
